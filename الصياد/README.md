
# دليل تحويل "الصياد" إلى برنامج EXE 🚀

اتبع هذه الخطوات داخل **Visual Studio Code** لتحويل مشروعك إلى برنامج ويندوز مستقل:

### 1. المتطلبات الأساسية
* تأكد من تثبيت **Node.js** على جهازك (حمله من [nodejs.org](https://nodejs.org/)).

### 2. تجهيز المجلد
* ضع جميع ملفات المشروع في مجلد واحد على جهازك.

### 3. تنفيذ الأوامر (في VS Code Terminal)
افتح الـ Terminal (اضغط `Ctrl + ` `) ثم اكتب الأوامر التالية:

```bash
# أولاً: تثبيت المكتبات اللازمة (يتم مرة واحدة فقط)
npm install

# ثانياً: تجربة تشغيل البرنامج للتأكد من أنه يعمل
npm start

# ثالثاً: إنشاء ملف الـ EXE النهائي
npm run dist
```

### 4. أين تجد البرنامج؟
بعد انتهاء أمر `npm run dist` (قد يستغرق دقيقة)، سيظهر مجلد جديد باسم **`dist`**. ستجد داخله ملف باسم **`AlSayyadPOS.exe`**.

---
**ملاحظة:** هذا الملف (AlSayyadPOS.exe) هو البرنامج النهائي، يمكنك نقله لأي جهاز ويندوز وسيعمل مباشرة!
